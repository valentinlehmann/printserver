# Native build-script decisions (pnpm blocks build scripts by default).
# better-sqlite3 is a native addon and must compile.
# sharp / unrs-resolver ship prebuilt binaries, so their scripts are skipped.
allowBuilds:
  better-sqlite3: true
  esbuild: true
  sharp: false
  unrs-resolver: false
